name: Deploy to GitHub Pages

on:
  push:
    branches: ['main']

  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: 'pages'
  cancel-in-progress: true

jobs:
  build:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
<<<<<<< HEAD
      - name: Checkout repository
        uses: actions/checkout@v4  # Node.js 20 を使用するバージョンに更新

      - name: Setup Node.js
        uses: actions/setup-node@v4  # Node.js 20 を使用するバージョンに更新
=======
      - name: Checkout
        uses: actions/checkout@v3
      - name: Setup Pages
        uses: actions/configure-pages@v3
      - name: Setup Node
        uses: actions/setup-node@v3
>>>>>>> parent of b2937cb (Update deploy.yml)
        with:
          node-version: 20
      - name: Install dependencies
        run: npm install
      - name: Build
        run: npm ci && npm run build
        env:
          PUBLIC_URL: /visiting-nursing-station-finder
<<<<<<< HEAD

      - name: Verify build contents  # 新規追加: ビルドディレクトリの内容を確認するステップ
        run: ls -la build

=======
>>>>>>> parent of b2937cb (Update deploy.yml)
      - name: Upload artifact
        uses: actions/upload-artifact@v4  # 最新バージョンに更新
        with:
          name: github-pages  # アーティファクトの名前を明示的に指定
          path: './build'

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Download artifact  # アーティファクトをダウンロードするステップを追加
        uses: actions/download-artifact@v4  # 最新バージョンに更新
        with:
          name: github-pages  # アップロード時と同じ名前を使用

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v2  # ここはNode.js 20対応バージョンがリリースされるまで待つ必要があります
